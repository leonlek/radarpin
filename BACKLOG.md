# Backlog

งานที่ตัดสินใจแล้วว่า "เอา" แต่ยังไม่ถึงเวลาทำ — เก็บรายละเอียดไว้ตรงนี้เพื่อไม่ต้องคิดใหม่

---

## 1. โฮสต์แผนที่เอง (PMTiles บน Cloudflare R2)

**สถานะ:** รอ — ทำเมื่อจะเริ่มขาย หรือเมื่อ OpenFreeMap ล่ม/ช้า/บล็อก

**ทำไม:** ตอนนี้ไทล์ทั้งหมดดึงจาก OpenFreeMap ซึ่งฟรีและอนุญาตงานเชิงพาณิชย์ แต่เป็น
เซิร์ฟเวอร์บริจาคของคนคนเดียว ไม่มีสัญญา ไม่มีการรับประกัน — ถ้าล่มแล้วลูกค้าที่จ่ายเงิน
เปิดมาเจอจอเปล่า เราทำอะไรไม่ได้เลย ส่วนที่เสี่ยงที่สุดคือฟีเจอร์แผนที่ออฟไลน์ เพราะเป็น
การดูดไทล์ทีละมาก ๆ

**ขั้นตอน**

1. `java -Xmx4g -jar planetiler.jar --download --area=thailand --output=thailand.pmtiles`
   (Planetiler โหลด extract ของ Geofabrik ให้เอง — ข้อมูลดิบมาจาก OSM ซึ่งเป็นแหล่ง
   เดียวกับที่ OpenFreeMap ใช้ ไม่ได้เอาไฟล์ต่อจากเขา) → ไฟล์เดียว ~1-2 GB
2. อัปขึ้น Cloudflare R2 (~1 บาท/เดือน, egress ฟรี) + Cloudflare Worker ตัวอย่างจาก
   โปรเจกต์ pmtiles แปลง `/{z}/{x}/{y}.mvt` เป็น range request บนไฟล์
   → ฝั่งแอปเห็นเป็น URL ไทล์ธรรมดา ออฟไลน์ยังทำงานเหมือนเดิม
3. ย้าย **style.json / glyphs / sprites** มาไว้บน R2 ด้วย — ตอนนี้ยืมของ OpenFreeMap อยู่
   ⚠️ **glyphs ต้องมีฟอนต์ไทย** ไม่งั้นชื่อสถานที่ภาษาไทยจะกลายเป็นกล่องว่าง
4. แก้ `STYLE_URL` — ตอนนี้มี **2 ที่** ควรยุบเหลือที่เดียวตอนทำ:
   - `ui/MapLibreMap.kt`
   - `offline/OfflineMaps.kt`

**ข้อควรรู้**

- แพ็กออฟไลน์ที่ผู้ใช้โหลดไว้แล้วผูกกับ style URL เดิม → เปลี่ยน URL = ต้องโหลดใหม่
- แผนที่จะไม่อัปเดตอัตโนมัติอีกต่อไป ต้องสร้างไฟล์ใหม่ทับเอง (เดือน/ไตรมาสละครั้งพอ)
- ไทล์ที่สร้างจาก OSM ยังอยู่ใต้ ODbL → เครดิต OpenStreetMap ต้องคงไว้ (ทำแล้ว) และถ้า
  แจกไฟล์ฐานข้อมูลต่อ ต้องเปิดเป็น ODbL
- ขั้นสร้างไฟล์กินดิสก์หลาย GB และ CPU เต็มไปสักพัก

---

## 2. การ์ด "ผ่านกล้องแล้ว ✓" ตอนขับเลยกล้องไป

**สถานะ:** รอ — วิเคราะห์ไว้แล้ว ยังไม่ลงมือ

**ปัญหา:** ตอนนี้พอเลยกล้อง ตัวกรองทิศ (มุมเกิน 115° = ขับหนีออก) ตัดจุดทิ้ง **แผงขวาหายวับทันที**

1. **ไม่มีการปิดจ๊อบ** — คนขับไม่รู้ว่าหายเพราะผ่านมาแล้ว หรือแอปเจ๊ง/GPS หลุด ความไม่แน่ใจ
   ทำให้ต้องละสายตาไปจ้องจอซ้ำ ซึ่งอันตรายกว่าตัวแบนเนอร์เอง
2. **จังหวะหายไม่แน่นอน** — ทิศจาก GPS แกว่งตอนความเร็วต่ำ ติดไฟแดงหน้ากล้องอาจกะพริบเข้า-ออก
3. **ถ้าปิดสวิตช์ "เตือนเฉพาะทิศเข้าหา" จะแย่กว่า** — แบนเนอร์ค้างต่อจนพ้นรัศมี 500 ม.
   ทั้งที่เลยกล้องไปแล้ว = เตือนสิ่งที่จบไปแล้ว

**ที่จะทำ:** แยก "ผ่านแล้ว" ออกจาก "กรองทิ้ง" ให้เป็นสถานะจริง

- กล้องเปลี่ยนจาก*ข้างหน้า* → *ข้างหลัง* ขณะรถวิ่ง = ผ่านแล้ว
- แผงขวาเปลี่ยนเป็นเทา/เขียวเรียบ ๆ ว่า **"ผ่านกล้องแล้ว ✓"** ค้าง 2-3 วิ แล้วหายเอง
- ไม่มีเสียง ไม่มีสีแดง — จุดประสงค์คือ "จบแล้ว มองถนนต่อ" ไม่ใช่เรียกความสนใจ
- ทำงานโดยไม่สนสวิตช์ "เตือนเฉพาะทิศเข้าหา" — สวิตช์นั้นควรคุมแค่ว่าจะเตือนจุดฝั่งตรงข้ามไหม
  ไม่ใช่ควบคุมว่าการเตือนจบยังไง
- กันกะพริบ: เข้าสถานะนี้ต้องรถวิ่งเกิน ~15 กม./ชม. และห้ามกลับไปเตือนซ้ำจนกว่าจะออกนอกรัศมี
  แล้วเข้ามาใหม่

⚠️ **ต้องทำ 2 ที่** — การเตือนมีโค้ดคนละชุดกัน และการ์ดนอกแอปเคยถูกลืมมาแล้วรอบหนึ่ง:
- ในแอป: `ui/MapScreen.kt` (`AlertRail` / `AlertBanner`)
- นอกแอป: `location/LocationService.kt` (`updateOverlay` + `showOverlay`, เป็น View ธรรมดา
  เพราะ service ไม่มี Compose) — ตอนนี้พอเลยกล้อง การ์ดก็แค่หายไปเงียบ ๆ เหมือนกัน

## 3. เรื่องอื่นก่อนขาย

- หน้า "ซอฟต์แวร์โอเพนซอร์สที่ใช้" ในแอป (BSD-2 ของ MapLibre กำหนดให้คงข้อความลิขสิทธิ์ไว้)
- ฐานกล้องที่ import จาก OSM เป็น ODbL — ถ้าจะแจก/ขายฐานที่ derive มา ต้องเปิดเป็น ODbL
  ทางที่สะอาดกว่าคือแยกไฟล์ระหว่างข้อมูลจาก OSM กับข้อมูลที่เราเก็บเอง
- เปลี่ยน `applicationId` จาก `com.bydmapcam` (เลี่ยงเครื่องหมายการค้า "BYD")
